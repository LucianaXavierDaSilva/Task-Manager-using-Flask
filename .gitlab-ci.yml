stages:
  - build

build:
  stage: build
  image: docker:20.10.21
  services:
    - docker:dind
  script:
    - echo "Compilando a imagem Docker..."
    - docker build --tag "meu-teste" .
    - echo "Imagem Docker construída com sucesso"

