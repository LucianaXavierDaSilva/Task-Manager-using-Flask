filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /logs/flask.log

output.elasticsearch:
  hosts: ["http://elasticsearch:9200"]

setup.kibana:
  host: "http://kibana:5601"

logging.level: info
